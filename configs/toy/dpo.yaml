schema_version: 1
profile: toy
algorithm:
  name: dpo
  variant: paper
  gamma: 1.0
  beta: 0.1
  label_smoothing: 0.0
  update_epochs: 1
  reward_source: verifier
data:
  id: tiny_reasoning
  revision: generated-v1
  split: train
  seed: 42
model:
  policy: tiny-v1
  reference: tiny-v1-frozen
training:
  seed: 42
  steps: 80
  batch_size: 16
  group_size: 4
  response_token_budget: 65536
  device: cpu
  allow_device_fallback: false
evaluation:
  every_steps: 20
  split: validation
output:
  root: artifacts
